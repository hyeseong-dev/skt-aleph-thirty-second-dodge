# 브라우저 검사 기록

## 검사 환경

- 검사 일시: 2026-09-07 11:07 KST
- 대상: 로컬 개발 서버 `http://localhost:3000/`
- 브라우저: Google Chrome Headless
- 검사 범위: 1366×768, 1920×1080, 연속 입력, 실행 중 크기 변경, 포커스 이탈 이벤트, 수동 일시정지, 검사 구간 콘솔 오류

## 검사 결과

| 항목 | 예상 결과 | 실제 결과 | 판정 |
| --- | --- | --- | :---: |
| 1366×768 | 가로 넘침과 주요 조작 영역 잘림이 없다. | 가로 넘침 0px. 게임판, 조작부, 정보 패널이 모두 화면 안에 있었다. | 통과 |
| 1920×1080 | 가로 넘침과 주요 조작 영역 잘림이 없다. | 가로 넘침 0px. 게임판, 조작부, 정보 패널이 모두 화면 안에 있었다. | 통과 |
| 연속 입력 | 1초 안에 오른쪽 방향키 입력 10회를 보내도 입력 누락·중복·고정 상태가 없다. | `keydown` 10회와 `keyup` 10회를 각각 정확히 수신했다. 플레이어가 오른쪽으로 100.2px 이동했고 마지막 `keyup` 후 정지했다. | 통과 |
| 실행 중 크기 변경 | 화면 크기를 바꿔도 진행 상태가 유지된다. | 1366×768에서 1920×1080으로 변경하기 전후 모두 `라운드 1` 진행 상태가 유지됐다. | 통과 |
| 포커스 이탈·복귀 | 이탈하면 시간이 멈추고, 복귀해도 사용자가 재개하기 전까지 정지 상태를 유지한다. | 브라우저 `blur`/`focus` 이벤트를 재현했다. 남은 시간이 58.7초로 유지됐고 이탈 후 일시정지, 복귀 후에도 일시정지가 유지됐다. | 통과 |
| 수동 일시정지 | 정지 중 시간이 흐르지 않고 재개 후 다시 흐른다. | 0.55초 대기 전후 모두 58.4초였고, 재개 후 58.1초로 감소했다. | 통과 |
| 콘솔 오류 | 위 검사 중 빨간 오류가 없다. | 파비콘 경로를 수정한 뒤 콘솔 오류 0건이었다. | 통과 |

## 아직 포함하지 않은 검사

- 10분 연속 실행 및 그 구간 전체의 콘솔 오류 검사
- 난이도 A/B 각각 10회의 사람 플레이 기록
- 정상·빈 값·손상된 저장값 복구 검사

이 세 검사는 별도의 장시간·반복·저장 시나리오이므로 이번 PC 경계 검사와 분리해 기록한다.
